#ifndef __queries__h
#define __queries__h

/*
** Genera un archivo CSV llamado "query1.csv" que contiene el total de multas por agencia y por infraccion.
** Retorna 1 si el archivo se generó correctamente, 0 en caso de error.
*/
// int query1(finesADT fines);

/*
** Genera un archivo CSV llamado "query2.csv" que contiene la recaudacion YDT por agencia.
** Retorna 1 si el archivo se generó correctamente, 0 en caso de error.
*/
// int query2(finesADT fines);

/**
** Genera un archivo CSV llamado "query3.csv" que contiene los montons minimos y maximos por agencia, y tambien
** la diferencia entre el maximo y el minimo.
** Retorna 1 si el archivo se generó correctamente, 0 en caso de error.
*/
// int query3(finesADT fines);

#endif